name: Deploy Bot

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build Container
        run: docker build -t bot .
      - name: Push to Registry
        run: echo "(placeholder para ECR/GCR)"
